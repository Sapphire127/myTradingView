// function dataAdaptation(data: string) {
//     const jsonObj = JSON.parse(jsonStr);

//     const result: string[] = [];

//     for (const key in jsonObj) {
//         if (jsonObj.hasOwnProperty(key)) {
//             jsonObj[key].time = key;
//             result.push(jsonObj[key]);
//         }
//     }
// }